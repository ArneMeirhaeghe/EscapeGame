Maak een nieuwe virtual environment op het nieuwe apparaat:

python3 -m venv venv
source venv/bin/activate

Installeer de libraries vanuit het requirements.txt-bestand:

pip install -r requirements.txt

